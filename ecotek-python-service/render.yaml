services:
  - type: web
    name: ecotek-python-engine
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host=0.0.0.0 --port=$PORT
    plan: free
    autoDeploy: true
